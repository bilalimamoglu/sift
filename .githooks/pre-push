#!/usr/bin/env bash
set -euo pipefail

if [[ "${SIFT_SKIP_PRE_PUSH_VERIFY:-0}" == "1" ]]; then
  echo "Skipping pre-push release-core verification because SIFT_SKIP_PRE_PUSH_VERIFY=1."
  exit 0
fi

echo "Running sift pre-push gate: npm run verify:release:core"
npm run verify:release:core
